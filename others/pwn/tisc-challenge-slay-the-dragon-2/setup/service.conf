service krpg
{
    disable     = no
    type        = UNLISTED
    wait        = no
    server      = /home/ctf/setup.py
    socket_type = stream
    protocol    = tcp
    user        = ctf
    port        = 4444
    flags       = REUSE
}
